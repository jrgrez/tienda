
<div class="form-group">
	<label >Buscar Categoría</label>
	<input type="Buscar" class="searc-category" id="buscar" placeholder="Buscar"l>
</div>

<table class="table table-hover"">
  <tr>
    <th>Producto</th>
    <th>Categoría</th> 
  </tr>
  	<% @products.each do |p| %>
  		<tr>
  		  	<td><%= p.name %></td>
  			<td><%= p.category.name %></td>
  		</tr>
  	<% end %>


</table>